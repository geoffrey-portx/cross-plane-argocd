apiVersion: v2
name: my-helm-chart
description: A Helm chart for deploying infrastructure and tenant environments
version: 0.1.0
dependencies:
  - name: infrastructure
    version: 0.1.0
    repository: "file://charts/infrastructure"  # Points to the local path of the infrastructure subchart
  - name: tenant-acme
    version: 0.1.0
    repository: "file://charts/tenant-acme"  # Points to the local path of the tenant-acme subchart

  